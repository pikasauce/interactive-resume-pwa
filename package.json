{
  "name": "release-automation-project",
  "version": "1.0.0",
  "private": true,
  "description": "An installable DevOps and Release Engineer resume PWA with its delivery system in the repository.",
  "scripts": {
    "start:api": "node services/resume-api/server.js",
    "start:ui": "node apps/resume-ui/server.js",
    "dev": "concurrently \"npm run start:api\" \"npm run start:ui\"",
    "lint": "node --check services/resume-api/server.js && node --check apps/resume-ui/server.js && node --check apps/resume-ui/app.js && node --check apps/resume-ui/service-worker.js",
    "test:e2e": "playwright test",
    "test:e2e:headed": "playwright test --headed",
    "ci:check": "npm run lint && npm run test:e2e"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.13.0",
    "@playwright/test": "^1.47.0",
    "concurrently": "^9.0.1"
  }
}
